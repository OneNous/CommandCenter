# ssh2 pulls optional `cpu-features` (native .node). It is built for *system* Node,
# but Electron bundles its own Node (different NODE_MODULE_VERSION) → load crash.
# Omit optional deps so ssh2 uses pure-JS crypto (fine for ICCP control traffic).
omit=optional
